# Core dependencies
requests==2.31.0
tqdm==4.66.1

# Email handling (standard library modules like smtplib, email, csv are not listed)
# No external dependency for these, already in Python's stdlib

# Selenium for automated catalog searches
selenium==4.11.2

# Optional: If you run in environments without system-installed Chrome, you may want webdriver-manager
webdriver-manager==4.0.0
